<Window x:Class="Tema_3.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autentificare" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <StackPanel Margin="30" VerticalAlignment="Center">
        <TextBlock Text="Email:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Padding="5"/>

        <TextBlock Text="Parolă:" Margin="0,0,0,5"/>
        <PasswordBox x:Name="TxtPassword" Margin="0,0,0,20" Padding="5"/>

        <Button Content="Conectare" Command="{Binding LoginCommand}" 
                CommandParameter="{Binding ElementName=TxtPassword}"
                Height="35" Background="DarkRed" Foreground="White" FontWeight="Bold"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </StackPanel>
</Window>